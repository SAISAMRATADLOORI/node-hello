pipeline{
    agent any
        stages{
            stage(Build){
                steps {
                    echo "this is a build"
                    sh 'npm install'
                }
            }
        }
    
}